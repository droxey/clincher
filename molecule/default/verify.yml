---
# Verify that variable validation and required-secret checks work correctly
- name: Verify variable validation
  hosts: localhost
  connection: local
  gather_facts: false

  tasks:
    - name: Confirm converge completed without errors
      ansible.builtin.assert:
        that: true
        success_msg: "Project-level validation and required-secret checks passed"
